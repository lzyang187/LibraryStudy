<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iflytek.librarystudy">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!-- <activity android:name=".gpuimage.GpuImageListActivity"></activity>-->
        <!-- <activity android:name=".gpuimage.GpuImageActivity"></activity> -->
        <!-- <activity android:name=".rxjava.operator.concat.RxJavaConcatActivity"></activity> -->
        <!-- <activity android:name=".rxjava.operator.map.RxJavaMapActivity"></activity> -->
        <!-- <activity android:name=".rxjava.operator.filter.UseFilterActivity"></activity> -->
        <!-- <activity android:name=".glide.recyclerview.GlideRecyclerViewActivity"></activity> -->
        <!-- <activity android:name=".glide.GlideTransformActivity"></activity> -->
        <!-- <activity android:name=".glide.GlideActivity"></activity> -->
        <!-- <activity android:name=".swipe.SwipeActivity"></activity> -->
        <!-- <activity android:name=".dagger2.DaggerThirdLibActivity"></activity> -->
        <!-- <activity android:name=".dagger2.Dagger2Activity"></activity> -->
        <!-- <activity android:name=".smartrefresh.SmartRefreshActivity"></activity> -->
        <!-- <activity android:name=".jsoup.JSoupActivity"></activity>-->
        <!-- <activity android:name=".greendao.ShopListActivity"></activity> -->
        <!-- <activity android:name=".rxjava.operator.RxJavaCreateActivity"></activity> -->
        <!-- <activity android:name=".dagger2.dependencies.ChildActivity"></activity> -->
        <!-- <activity android:name=".dagger2.ParkActivity"></activity> -->
        <!-- <activity android:name=".jobscheduler.JobActivity" /> -->
        <!-- <activity android:name=".rxjava.use.IntervalActivity"></activity> -->
        <!-- <activity android:name=".rxjava.RxJavaCreateActivity"></activity> -->
        <activity android:name=".rxjava.operator.filter.UseFilterActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".jobscheduler.MyJobService"
            android:permission="android.permission.BIND_JOB_SERVICE" />
    </application>

</manifest>