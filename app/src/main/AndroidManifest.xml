<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iflytek.librarystudy">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".greendao.ShopListActivity"></activity>
        <!-- <activity android:name=".rxjava.operator.RxJavaCreateActivity"></activity> -->
        <!-- <activity android:name=".dagger2.dependencies.ChildActivity"></activity> -->
        <!-- <activity android:name=".dagger2.ParkActivity"></activity> -->
        <activity android:name=".jobscheduler.JobActivity" /> <!-- <activity android:name=".rxjava.use.IntervalActivity"></activity> -->
        <!-- <activity android:name=".rxjava.RxJavaCreateActivity"></activity> -->
        <activity android:name=".rxjava.operator.RxJavaCreateActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".jobscheduler.MyJobService"
            android:permission="android.permission.BIND_JOB_SERVICE" />
    </application>
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

</manifest>